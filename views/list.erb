<!DOCTYPE HTML>
<html>
  <head>
    <title>Bookmarks</title>
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type">
    <link href="/stylesheets/memo.css" type="text/css" rel="stylesheet">
    <link href="/stylesheets/bootstrap.css" rel="stylesheet" media="screen">
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  </head>
  <body style="margin:5pt;">
    <h3><i>List of QuickBM bookmarks</i></h3>

    <div class="container">
      <div class="span11">
	<table class="table table-striped table-hover table-bordered table-condensed">
          <% @data.each { |e| %>
            <% if e['longname'].to_s != '' && e['name'] != '' %>
              <tr>
                <td class="span2"><a href="/<%= e['name'] %>!"><%= e['name'] %></a></td>
                <% if e['longname'] =~ /^http/ %>
                  <td><a href="%<= e['longname'] %>"><%= e['longname'] %></a></td>
                <% else %>
                  <td><%= e['longname'] %></td>
                <% end %>
              </tr>
            <% end %>
          <% } %>
  	</table>
      </div>
    </div>
	  
    <hr>

    <%= erb :form %>
    
  </body>
</html>
